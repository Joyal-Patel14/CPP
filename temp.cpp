#include <bits/stdc++.h>
using namespace std;


int main()
{
    ios_base::sync_with_stdio(NULL);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int n; cin >> n;
    int a[n];

    for(int i=0; i<n; i++) cin >> a[i];

    int counter = 0;

    while(counter < n) {
        for(int i=0; i<n; i++) {
            if(a[i] > a[i+1]) {

            }
        }
    }

    return 0;
}
