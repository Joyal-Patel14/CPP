#include <bits/stdc++.h>
using namespace std;

// Fermat's Little theorem: It states that if p is prime number, then for any integer a, the number a^p - a is an integer multiole of p.
// a^p = a(mod p)
// Wilson's theorem: A positive integer n>1 is a prime if and omly if (n-1) != -1(mod n) OR (n-1) != n-1 (mod n)

int main()
{
    


    return 0;
}
